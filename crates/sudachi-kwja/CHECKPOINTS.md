# Checkpoint hashes

Generated by `scripts/convert_checkpoints.py` from KWJA upstream v2.4.
Used by `tests/equivalence/` to detect drift if the source `.ckpt` ever changes.

## v0.1 (char + word)

| File | sha256 | Size |
|---|---|---|
| char.safetensors | `2ed70587bf52c41e3bb4fe017724e504c9e94f1d3b5c184286e0dae516ab0f2f` | 423,657,688 |
| word.safetensors | `14106e492b4cdbea912b254ca06caedb63310010505f73241ea4fe5b9b471185` | 532,118,228 |

## Tokenizer auxiliary files

| File | Source |
|---|---|
| `kwja-tokenizer/tokenizer.json` | HF `ku-nlp/deberta-v2-base-japanese`, used by word module |
| `kwja-char/vocab.txt` | HF `ku-nlp/deberta-v2-base-japanese-char-wwm`, used by char module |
| `kwja-char/tokenizer_config.json` | matched config for char vocab |

## Out of scope

Typo module (`typo.safetensors`) is deferred to v0.2 — the production cache only ships char + word.

## Source

- Container: `jisho-parse-1` on htpc
- Cache: `/root/.cache/kwja/v2.4/{char_deberta-v2-base-wwm,word_deberta-v2-base}.ckpt`
- HuggingFace tokenizer cache: `/root/.cache/huggingface/models--ku-nlp--deberta-v2-base-japanese{,-char-wwm}/`

## Deploy paths

- htpc:`~/.local/share/jisho/checkpoints/`
- dev macbook (test runs): `~/.local/share/jisho/checkpoints/` (rsynced from htpc)

The Dockerfile bind-mounts `~/.local/share/jisho/checkpoints/` → `/checkpoints/` inside the container.
